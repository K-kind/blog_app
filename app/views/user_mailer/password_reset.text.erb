Progress

<%= @user.name %>さん、当ブログをご利用いただきありがとうございます。

下のリンクからパスワードリセット用画面に遷移していただくと、任意のパスワードを再設定することができます。

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

このリンクの有効期限はは2時間です。

もしパスワードをリセットされない場合は、このメールは無視していただいて構いません。現在のパスワードはそのままお使いいただけます。